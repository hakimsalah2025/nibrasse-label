# 🚀 دليل التشغيل السريع - NIBRASSE

## ⚡ التشغيل السريع

### 🎯 الطريقة الأسهل (تشغيل + فتح المتصفح تلقائياً):
```
نقرة مزدوجة على: quick_start.bat
```
✨ **سيتم فتح المتصفح تلقائياً على http://localhost:8000**

### 🔧 الطريقة الكاملة (مع فحوصات):
```
نقرة مزدوجة على: start_app.bat
```
✨ **المتصفح سيفتح تلقائياً بعد 3 ثواني**
ℹ️ **ملاحظة:** قد يستغرق البدء بضع ثوانٍ إضافية لإعادة بناء فهرس البحث من قاعدة البيانات (لضمان عدم ضياع البيانات).

### ⏹️ لإيقاف التطبيق:
```
نقرة مزدوجة على: stop_app.bat
```


---

## 📋 المتطلبات

قبل التشغيل، تأكد من:

✅ **Python 3.10+** مثبت على النظام  
✅ جميع المكتبات المطلوبة مثبتة (راجع `requirements.txt`)  
✅ ملف `.env` موجود في مجلد `backend/` مع المفاتيح السرية  

---

## 🔧 التثبيت الأولي

إذا كنت تشغل التطبيق لأول مرة:

### 1. تثبيت المكتبات:
```cmd
pip install -r requirements.txt
```

### 2. إعداد ملف .env:
انسخ المفاتيح السرية إلى `backend/.env`:
```env
GEMINI_API_KEY=your_gemini_api_key_here
VITE_SUPABASE_URL=your_supabase_url_here
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here
VITE_GEMINI_CHAT_MODEL=gemini-1.5-pro-002
VITE_GEMINI_EMBEDDING_MODEL=models/embedding-001
```

### 3. شغّل التطبيق:
```cmd
start_app.bat
```

---

## 🌐 الوصول للتطبيق

بعد التشغيل، افتح المتصفح وانتقل إلى:

**http://localhost:8000**

---

## 📝 ملاحظات مهمة

### ⚠️ إيقاف التطبيق
- **الطريقة الأولى (الأفضل):** `stop_app.bat`
- **الطريقة الثانية:** في نافذة Terminal، اضغط `Ctrl+C`

### 🔄 إعادة التشغيل
إذا واجهت مشاكل:
1. شغّل `stop_app.bat`
2. انتظر 5 ثواني
3. شغّل `start_app.bat`

### 🐛 حل المشاكل

**المشكلة:** "Port 8000 is already in use"  
**الحل:** 
```cmd
stop_app.bat
```
ثم حاول مرة أخرى

**المشكلة:** "Module not found"  
**الحل:**
```cmd
pip install -r requirements.txt
```

**المشكلة:** التطبيق لا يفتح  
**الحل:**
1. تأكد من ملف `.env`
2. راجع رسائل الخطأ في Terminal
3. تأكد من اتصال الإنترنت (مطلوب لـ Gemini API)

---

## 📚 مراجع إضافية

- **الدليل الفني:** `DOCUMENTATION_TECHNIQUE_FR.md`
- **دليل الاستخدام:** `GUIDE_UTILISATION_FR.md`
- **تحليل النظام:** `ANALYSE_COMPLETE_SYSTEME.md`

---

## 💡 نصائح

✨ **للتطوير:** استخدم `--reload` (مفعّل افتراضياً) لإعادة تحميل التغييرات تلقائياً  
✨ **للإنتاج:** عدّل `start_app.bat` وأزل `--reload`  
✨ **للأداء:** أغلق البرامج الثقيلة الأخرى أثناء التشغيل  

---

**تم إنشاء الملفات بواسطة:** Antigravity AI  
**التاريخ:** 25 نوفمبر 2025
